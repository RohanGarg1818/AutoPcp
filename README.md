# Autopcp prototype

Open `index.html` in a browser, or publish the repository with GitHub Pages.

The page accepts pasted Python code and performs local checks for common security and style issues. The code is not uploaded anywhere.

`autopcp_scan.py` is the separate local Python AST scanner for command-line use.
